const Name="Kartik"; //Variable cannot be re-declared or updated. A block scope variable.

let age=18;    //Variable cannot be re-declared but can be updated

var char='c';          // Variable can be re-declared & updated

id=12;        //Global Variable

//Name="Sahil";  Not allowed we can not change const variable value
console.log(Name);

age = 20;

char='B';

id=101;

console.table([Name,age,char,id]);